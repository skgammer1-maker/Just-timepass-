<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>💕 A Question For You...</title>
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Nunito:wght@400;600;700&display=swap" rel="stylesheet"/>
  <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
  
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --pink: #ff6b9d;
      --deep-pink: #e0527e;
      --blush: #ffcce0;
      --rose: #ff3e79;
      --cream: #fff5f8;
      --text: #7a2e4e;
    }

    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #ffe0ec 0%, #ffd6e7 40%, #fce4ec 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Nunito', sans-serif;
      overflow: hidden;
      position: relative;
    }

    /* Floating hearts background */
    .bg-hearts {
      position: fixed;
      inset: 0;
      pointer-events: none;
      z-index: 0;
    }
    .bg-heart {
      position: absolute;
      font-size: 1.5rem;
      opacity: 0;
      animation: floatUp linear infinite;
    }

    @keyframes floatUp {
      0%   { transform: translateY(110vh) rotate(0deg); opacity: 0; }
      10%  { opacity: 0.5; }
      90%  { opacity: 0.3; }
      100% { transform: translateY(-10vh) rotate(30deg); opacity: 0; }
    }

    /* Card */
    .card {
      position: relative;
      z-index: 10;
      background: rgba(255,255,255,0.75);
      backdrop-filter: blur(16px);
      border: 2px solid rgba(255,107,157,0.3);
      border-radius: 2.5rem;
      padding: 3rem 2.5rem 2.5rem;
      text-align: center;
      max-width: 480px;
      width: 90%;
      box-shadow: 0 20px 60px rgba(255,62,121,0.18), 0 4px 20px rgba(255,107,157,0.15);
      transition: all 0.5s ease-in-out;
    }

    /* ── SCREEN 1 ── */
    #screen-ask { display: block; }

    .heart-svg-wrap {
      position: relative;
      width: 200px;
      height: 185px;
      margin: 0 auto 1.4rem;
    }
    .heart-svg-wrap svg {
      width: 100%;
      height: 100%;
      filter: drop-shadow(0 6px 24px rgba(255,62,121,0.35));
      animation: heartbeat 1.4s ease-in-out infinite;
    }
    @keyframes heartbeat {
      0%,100% { transform: scale(1); }
      14%      { transform: scale(1.12); }
      28%      { transform: scale(1); }
      42%      { transform: scale(1.07); }
      56%      { transform: scale(1); }
    }

    .heart-label {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 2px;
    }
    .heart-label .emoji-row {
      font-size: 1.5rem;
      letter-spacing: 3px;
    }
    .heart-label .script-text {
      font-family: 'Great Vibes', cursive;
      font-size: 1.18rem;
      color: #fff;
      text-shadow: 0 1px 4px rgba(180,0,60,0.4);
      line-height: 1.3;
    }

    .question {
      font-family: 'Great Vibes', cursive;
      font-size: 2.6rem;
      color: var(--text);
      margin-bottom: 0.4rem;
      line-height: 1.2;
      text-shadow: 0 2px 8px rgba(255,107,157,0.2);
    }
    .subtext {
      font-size: 0.9rem;
      color: #c0698e;
      margin-bottom: 2rem;
      font-weight: 600;
      letter-spacing: 0.04em;
    }

    .btn-row {
      display: flex;
      gap: 1.5rem;
      justify-content: center;
      align-items: center;
      position: relative;
      min-height: 70px;
    }

    .btn-yes, .btn-no {
      padding: 0.75rem 2.2rem;
      border: none;
      border-radius: 999px;
      font-family: 'Nunito', sans-serif;
      font-size: 1.05rem;
      font-weight: 700;
      cursor: pointer;
      transition: transform 0.15s, box-shadow 0.15s;
    }
    .btn-yes {
      background: linear-gradient(135deg, #ff6b9d, #ff3e79);
      color: #fff;
      box-shadow: 0 6px 20px rgba(255,62,121,0.4);
      z-index: 11;
    }
    .btn-yes:hover {
      transform: scale(1.08);
      box-shadow: 0 8px 28px rgba(255,62,121,0.55);
    }
    .btn-no {
      background: #fff;
      color: var(--pink);
      border: 2px solid var(--pink);
      transition: transform 0.15s;
      z-index: 12;
    }

    /* ── SCREEN 2 (Prank) & SCREEN 3 (Real) ── */
    #screen-prank, #screen-real { display: none; }

    .prank-emoji, .real-emoji {
      font-size: 4.5rem;
      display: block;
      margin-bottom: 0.8rem;
    }
    .prank-emoji { animation: wiggle 0.6s ease-in-out infinite alternate; }
    .real-emoji { animation: pulseHeart 0.8s ease-in-out infinite alternate; }

    @keyframes wiggle {
      from { transform: rotate(-10deg) scale(1); }
      to   { transform: rotate(10deg) scale(1.1); }
    }
    @keyframes pulseHeart {
      from { transform: scale(1); filter: drop-shadow(0 2px 5px rgba(255,62,121,0.2)); }
      to   { transform: scale(1.15); filter: drop-shadow(0 5px 15px rgba(255,62,121,0.4)); }
    }

    .prank-title, .real-title {
      font-family: 'Great Vibes', cursive;
      font-size: 3rem;
      color: var(--rose);
      text-shadow: 0 2px 12px rgba(255,62,121,0.3);
      margin-bottom: 0.5rem;
    }
    .prank-sub, .real-sub {
      font-size: 1rem;
      color: #c0698e;
      font-weight: 600;
      margin-bottom: 1.5rem;
      line-height: 1.6;
    }

    .roast-alert {
      font-size: 0.95rem;
      font-weight: 700;
      color: #b83b66;
      background: rgba(255, 107, 157, 0.15);
      padding: 0.6rem 1rem;
      border-radius: 12px;
      margin-bottom: 1.5rem;
      display: inline-block;
    }

    .action-container {
      display: flex;
      flex-direction: column;
      gap: 0.8rem;
      align-items: center;
      margin-top: 1rem;
    }

    .btn-reveal {
      background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
      color: white;
      padding: 0.75rem 2.2rem;
      border: none;
      border-radius: 999px;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 5px 15px rgba(0,242,254,0.4);
      transition: transform 0.2s;
    }
    .btn-reveal:hover { transform: scale(1.05); }

    .reset-btn {
      padding: 0.5rem 1.5rem;
      border: 2px solid var(--pink);
      background: transparent;
      color: var(--pink);
      border-radius: 999px;
      font-family: 'Nunito', sans-serif;
      font-weight: 700;
      font-size: 0.85rem;
      cursor: pointer;
      transition: all 0.2s;
    }
    .reset-btn:hover {
      background: var(--pink);
      color: #fff;
    }

    .love-signature {
      font-family: 'Great Vibes', cursive;
      font-size: 2.2rem;
      color: var(--rose);
      margin-top: 1rem;
    }
  </style>
</head>
<body>

  <div class="bg-hearts" id="bgHearts"></div>

  <div class="card">

    <div id="screen-ask">
      <div class="heart-svg-wrap">
        <svg viewBox="0 0 200 185" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <linearGradient id="hg" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%"   stop-color="#ff8fb5"/>
              <stop offset="100%" stop-color="#ff2d6e"/>
            </linearGradient>
          </defs>
          <path d="M100 170 C60 140, 10 110, 10 65
                   C10 35, 35 15, 60 15
                   C75 15, 90 23, 100 35
                   C110 23, 125 15, 140 15
                   C165 15, 190 35, 190 65
                   C190 110, 140 140, 100 170Z"
                fill="url(#hg)"/>
          <ellipse cx="72" cy="55" rx="18" ry="11" fill="rgba(255,255,255,0.25)" transform="rotate(-20,72,55)"/>
        </svg>
        <div class="heart-label">
          <div class="emoji-row">💕✨💕</div>
          <div class="script-text">Will you be</div>
          <div class="script-text">my Girlfriend?</div>
        </div>
      </div>

      <p class="question">Will You Be My Girlfriend?</p>
      <p class="subtext">Choose wisely… or not 😉</p>

      <div class="btn-row" id="btnRow">
        <button class="btn-yes" id="btnYes" onclick="handleYes()">💖 Yes!</button>
        <button class="btn-no" id="btnNo" onmouseenter="runAway()" ontouchstart="runAway()" onclick="handleNopeClick()">Nope 🙅</button>
      </div>
    </div>

    <div id="screen-prank">
      <span class="prank-emoji">😂</span>
      <p class="prank-title">Hahaha… PRANK!</p>
      <p class="prank-sub">
        Gotcha! 😜<br/>
        You actually thought this was real? 🤣
      </p>
      <div id="roastText" class="roast-alert"></div>
      
      <div class="action-container">
        <button class="btn-reveal" onclick="showRealScreen()">Wait, for real? 🥺</button>
        <button class="reset-btn" onclick="resetGame()">↺ Play Again</button>
      </div>
    </div>

    <div id="screen-real">
      <span class="real-emoji">🌹❤️</span>
      <p class="real-title">Okay, I'm done teasing...</p>
      <p class="real-sub">
        The prank screen was just me being nervous. But my feelings are 100% genuine. You make my world incredibly bright, and I'd love nothing more than to make it official.
      </p>
      <div class="love-signature">Forever Yours ✨</div>
    </div>

  </div>

  <script>
    /* ── Floating background hearts ── */
    const bgHearts = document.getElementById('bgHearts');
    const emojis = ['💕','💗','💖','🌸','💓','✨','🌷','💝'];
    for (let i = 0; i < 22; i++) {
      const el = document.createElement('div');
      el.className = 'bg-heart';
      el.textContent = emojis[Math.floor(Math.random() * emojis.length)];
      el.style.left = Math.random() * 100 + 'vw';
      el.style.animationDuration = (6 + Math.random() * 10) + 's';
      el.style.animationDelay = (Math.random() * 10) + 's';
      el.style.fontSize = (1 + Math.random() * 1.5) + 'rem';
      bgHearts.appendChild(el);
    }

    /* ── Audio Engine ── */
    let audioCtx = null;
    function initAudio() {
      if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)();
    }

    function playSfx(type) {
      try {
        initAudio();
        if (!audioCtx) return;
        
        const osc = audioCtx.createOscillator();
        const gain = audioCtx.createGain();
        osc.connect(gain);
        gain.connect(audioCtx.destination);

        if (type === 'boing') {
          osc.type = 'sine';
          osc.frequency.setValueAtTime(150, audioCtx.currentTime);
          osc.frequency.exponentialRampToValueAtTime(400, audioCtx.currentTime + 0.12);
          gain.gain.setValueAtTime(0.15, audioCtx.currentTime);
          gain.gain.exponentialRampToValueAtTime(0.01, audioCtx.currentTime + 0.12);
          osc.start();
          osc.stop(audioCtx.currentTime + 0.12);
        } else if (type === 'chime') {
          const notes = [261.63, 329.63, 392.00, 523.25];
          notes.forEach((freq, idx) => {
            const o = audioCtx.createOscillator();
            const g = audioCtx.createGain();
            o.connect(g);
            g.connect(audioCtx.destination);
            o.type = 'triangle';
            o.frequency.setValueAtTime(freq, audioCtx.currentTime + (idx * 0.08));
            g.gain.setValueAtTime(0.1, audioCtx.currentTime + (idx * 0.08));
            g.gain.exponentialRampToValueAtTime(0.005, audioCtx.currentTime + (idx * 0.08) + 0.3);
            o.start(audioCtx.currentTime + (idx * 0.08));
            o.stop(audioCtx.currentTime + (idx * 0.08) + 0.3);
          });
        }
      } catch (e) { console.log("Audio blocked"); }
    }

    /* ── Runaway Button Engine ── */
    const btnNo = document.getElementById('btnNo');
    let noEscapes = 0;

    function runAway() {
      if (noEscapes >= 5) {
        // On the 6th interaction, stop moving and accept defeat
        noEscapes = 6;
        btnNo.textContent = "Fine, click me! 😤";
        btnNo.style.cursor = "pointer";
        return;
      }
      
      noEscapes++;
      playSfx('boing');
      
      const card = document.querySelector('.card');
      const cardRect = card.getBoundingClientRect();
      const btnRect = btnNo.getBoundingClientRect();

      btnNo.style.position = 'fixed';

      const maxX = cardRect.width  - btnRect.width  - 30;
      const maxY = cardRect.height - btnRect.height - 30;

      const newX = Math.floor(Math.random() * maxX) + 15;
      const newY = Math.floor(Math.random() * maxY) + 15;

      btnNo.style.left = (cardRect.left + newX) + 'px';
      btnNo.style.top  = (cardRect.top  + newY) + 'px';

      const yesBtn = document.getElementById('btnYes');
      const scale = 1 + noEscapes * 0.08;
      yesBtn.style.transform = `scale(${Math.min(scale, 1.7)})`;

      const taunts = [
        'Nope 🙅','Still no!','Never!','Stop chasing!','😅 Nah'
      ];
      btnNo.textContent = taunts[(noEscapes - 1) % taunts.length];
    }

    /* ── Page Flow Control ── */
    function handleYes() {
      confetti({ particleCount: 60, spread: 40, origin: { y: 0.6 } });
      playSfx('chime');

      const roastText = document.getElementById('roastText');
      if (noEscapes === 0) {
        roastText.textContent = "Wow, zero hesitation! You completely passed the test. 🥰";
      } else {
        roastText.textContent = `You gave up on "Nope" after ${noEscapes} tries and picked Yes! Wise choice. 😂`;
      }

      setTimeout(() => {
        document.getElementById('screen-ask').style.display = 'none';
        document.getElementById('screen-prank').style.display = 'block';
      }, 300);
    }

    function handleNopeClick() {
      // Only process the click if they have reached the 6th attempt limits
      if (noEscapes < 6) return;

      confetti({ particleCount: 60, spread: 40, origin: { y: 0.6 } });
      playSfx('chime');

      const roastText = document.getElementById('roastText');
      roastText.textContent = "You stubbornly clicked 'Nope' 6 times until the button literally gave up! 😂";

      setTimeout(() => {
        document.getElementById('screen-ask').style.display = 'none';
        document.getElementById('screen-prank').style.display = 'block';
      }, 300);
    }

    function showRealScreen() {
      playSfx('chime');
      confetti({ particleCount: 180, spread: 80, scalar: 1.2 });
      
      document.getElementById('screen-prank').style.display = 'none';
      document.getElementById('screen-real').style.display = 'block';
    }

    function resetGame() {
      noEscapes = 0;
      btnNo.style.position = 'static'; 
      btnNo.style.left = btnNo.style.top = '';
      btnNo.style.cursor = 'not-allowed';
      btnNo.textContent = 'Nope 🙅';
      document.getElementById('btnYes').style.transform = '';
      document.getElementById('screen-ask').style.display = 'block';
      document.getElementById('screen-prank').style.display = 'none';
      document.getElementById('screen-real').style.display = 'none';
    }
  </script>
</body>
</html>
